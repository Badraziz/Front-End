export class Authority {

  authorityId: number;
  authority: string;


  constructor(authorityId: number, authority: string) {
    this.authorityId = authorityId;
    this.authority = authority;
  }

}
